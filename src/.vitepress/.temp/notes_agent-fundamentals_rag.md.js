import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"RAG：从检索到生成","description":"理解 RAG 的索引、召回、重排、上下文注入与评测链路。","frontmatter":{"title":"RAG：从检索到生成","description":"理解 RAG 的索引、召回、重排、上下文注入与评测链路。","outline":[2,3]},"headers":[],"relativePath":"notes/agent-fundamentals/rag.md","filePath":"notes/agent-fundamentals/rag.md","lastUpdated":1788335820000}');
const _sfc_main = { name: "notes/agent-fundamentals/rag.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="rag-从检索到生成" tabindex="-1">RAG：从检索到生成 <a class="header-anchor" href="#rag-从检索到生成" aria-label="Permalink to &quot;RAG：从检索到生成&quot;">​</a></h1><blockquote><p>RAG（Retrieval-Augmented Generation，检索增强生成）的核心思想，是在生成答案前先从外部知识源中检索相关证据，再把证据作为上下文交给大模型。它不修改模型参数，而是通过动态补充信息，让模型能够使用更新后的知识、私有资料和可核验的依据。</p></blockquote><h2 id="_1-基础概念" tabindex="-1">1. 基础概念 <a class="header-anchor" href="#_1-基础概念" aria-label="Permalink to &quot;1. 基础概念&quot;">​</a></h2><h3 id="_1-1-解决的问题" tabindex="-1">1.1 解决的问题 <a class="header-anchor" href="#_1-1-解决的问题" aria-label="Permalink to &quot;1.1 解决的问题&quot;">​</a></h3><p>RAG 主要缓解三个问题：</p><ul><li><strong>知识时效性</strong>：知识库可以独立更新，不受模型训练截止时间限制；</li><li><strong>私有数据接入</strong>：企业文档、项目代码和用户资料无须进入训练集；</li><li><strong>无依据生成</strong>：模型可以基于检索证据回答并给出来源，但 RAG 只能降低幻觉，不能保证答案一定正确。</li></ul><h3 id="_1-2-处理流程" tabindex="-1">1.2 处理流程 <a class="header-anchor" href="#_1-2-处理流程" aria-label="Permalink to &quot;1.2 处理流程&quot;">​</a></h3><p>一套典型 RAG 系统包含离线索引和在线问答两条链路：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>离线索引</span></span>
<span class="line"><span>文档 → 解析 → 切片 → 元数据 → Embedding / FTS → 索引</span></span>
<span class="line"><span></span></span>
<span class="line"><span>在线问答</span></span>
<span class="line"><span>用户问题 → Query 构造 → 召回 → 融合 → 重排</span></span>
<span class="line"><span>        → 选择证据 → 注入上下文 → LLM 生成</span></span></code></pre></div><p>离线阶段决定知识以什么粒度被存储，在线阶段决定哪些知识能够进入当前上下文。生成效果的上限往往受检索结果限制：正确证据没有被召回，后面的模型再强也无法可靠利用它。</p><h3 id="_1-3-概念关系" tabindex="-1">1.3 概念关系 <a class="header-anchor" href="#_1-3-概念关系" aria-label="Permalink to &quot;1.3 概念关系&quot;">​</a></h3><p>RAG 中常见术语并不处于同一层：</p><table tabindex="0"><thead><tr><th>概念</th><th>类型</th><th>所在环节</th></tr></thead><tbody><tr><td><strong>FTS</strong></td><td>全文检索能力</td><td>建立和查询关键词索引</td></tr><tr><td><strong>倒排索引</strong></td><td>关键词索引结构</td><td>记录词项出现在哪些文档中</td></tr><tr><td><strong>BM25</strong></td><td>稀疏检索排序算法</td><td>对关键词匹配结果计算相关性</td></tr><tr><td><strong>Embedding</strong></td><td>向量表示</td><td>将 Query 和 Chunk 转换为向量</td></tr><tr><td><strong>Bi-Encoder</strong></td><td>独立编码架构</td><td>支持大规模语义召回</td></tr><tr><td><strong>ANN</strong></td><td>近似向量搜索方法</td><td>从向量索引快速寻找候选</td></tr><tr><td><strong>RRF</strong></td><td>排名融合算法</td><td>合并关键词与向量检索结果</td></tr><tr><td><strong>Cross-Encoder</strong></td><td>联合编码模型</td><td>对少量候选进行精确重排</td></tr></tbody></table><h2 id="_2-知识库构建" tabindex="-1">2. 知识库构建 <a class="header-anchor" href="#_2-知识库构建" aria-label="Permalink to &quot;2. 知识库构建&quot;">​</a></h2><h3 id="_2-1-加载与解析" tabindex="-1">2.1 加载与解析 <a class="header-anchor" href="#_2-1-加载与解析" aria-label="Permalink to &quot;2.1 加载与解析&quot;">​</a></h3><p>Load 负责从 PDF、网页、数据库、Notion 或代码仓库读取原始资料；Parse 则把不同格式转换成后续可以切片和索引的统一表示。</p><p>解析不能只保留纯文本，还应尽量保留标题层级、页码、表格边界、代码符号、图片位置和访问权限。否则即使召回了正确内容，系统也可能无法解释来源，或者把本应属于不同章节的信息混在一起。</p><h3 id="_2-2-多模态解析" tabindex="-1">2.2 多模态解析 <a class="header-anchor" href="#_2-2-多模态解析" aria-label="Permalink to &quot;2.2 多模态解析&quot;">​</a></h3><p>并非所有知识都能直接转换成连续文本：</p><ul><li><strong>PDF</strong>：电子 PDF 可以直接抽取文本；双栏、表格和复杂排版需要布局感知解析；扫描件需要 OCR 或视觉模型；</li><li><strong>表格</strong>：可以保留为 Markdown 或 JSON；涉及筛选和聚合时，更适合存入 DataFrame 或数据库后通过查询工具访问；</li><li><strong>图片</strong>：辅助图片可以生成 Caption 后进入文本索引；图片本身就是主要信息时，需要使用多模态 Embedding；</li><li><strong>代码</strong>：优先按类、函数和符号解析，并保留文件路径、行号和依赖关系。</li></ul><p>无论采用 OCR、布局模型还是视觉模型，都应保留原始文件、页码或图片引用。解析结果可能遗漏版式，也可能产生识别错误，不能把转换后的文本当作唯一事实来源。</p><h3 id="_2-3-文档切片" tabindex="-1">2.3 文档切片 <a class="header-anchor" href="#_2-3-文档切片" aria-label="Permalink to &quot;2.3 文档切片&quot;">​</a></h3><p>切片（Chunking）是把长文档拆成可检索片段。之所以需要切片，主要有三个原因：</p><ul><li>Embedding 模型能够处理的输入长度有限；</li><li>大模型的上下文窗口无法容纳完整知识库；</li><li>文档粒度过大会稀释局部主题，粒度过小又会丢失必要语境。</li></ul><p>常见策略如下：</p><table tabindex="0"><thead><tr><th>方式</th><th>做法</th><th>优点</th><th>局限</th></tr></thead><tbody><tr><td><strong>固定长度</strong></td><td>按字符或 Token 数切分</td><td>简单、稳定</td><td>可能切断句子和语义</td></tr><tr><td><strong>重叠切片</strong></td><td>相邻 Chunk 保留一段重叠内容</td><td>缓解边界信息丢失</td><td>增加的成本取决于重叠比例</td></tr><tr><td><strong>递归切片</strong></td><td>依次尝试章节、段落、句子等分隔符</td><td>尽量保持自然边界</td><td>依赖文档格式质量</td></tr><tr><td><strong>结构切片</strong></td><td>按标题、条款、API、类或函数切分</td><td>片段语义更完整</td><td>需要格式或语法解析</td></tr><tr><td><strong>语义切片</strong></td><td>在主题相似度明显变化处切分</td><td>能识别话题转折</td><td>计算成本高，边界未必稳定</td></tr><tr><td><strong>Agentic 切片</strong></td><td>由模型根据任务含义决定边界</td><td>能处理复杂文档</td><td>成本高且结果不完全确定</td></tr></tbody></table><p>不存在适用于所有资料的固定 <code>chunk_size</code>。更稳妥的原则是：让一个 Chunk 尽量成为能够独立回答某类问题的最小语义单元。</p><p>每个 Chunk 还应携带必要的元数据：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>document_id / title / heading_path</span></span>
<span class="line"><span>file_path / page / line_range</span></span>
<span class="line"><span>version / timestamp / permissions</span></span></code></pre></div><p>切片后容易出现“片段脱离原文”的问题。例如，“该方案降低了 30% 延迟”没有说明具体方案和测试范围。<a href="https://www.anthropic.com/news/contextual-retrieval" target="_blank" rel="noreferrer">Anthropic Contextual Retrieval</a> 会在索引前为 Chunk 添加简短的文档级背景，再对“背景 + 原始片段”建立语义和关键词索引。</p><h3 id="_2-4-关键词索引" tabindex="-1">2.4 关键词索引 <a class="header-anchor" href="#_2-4-关键词索引" aria-label="Permalink to &quot;2.4 关键词索引&quot;">​</a></h3><p>**FTS（Full-Text Search，全文检索）**不是某一种具体算法，而是一套针对文本内容建立索引、执行查询并计算相关性的检索能力。它与逐行扫描或 SQL <code>LIKE &#39;%keyword%&#39;</code> 的主要区别，是提前建立了可供查询的索引。</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>文档 → 分词与规范化 → 倒排索引</span></span>
<span class="line"><span>Query → 同样的分词处理 → 查找候选 → 相关性排序</span></span></code></pre></div><p><strong>分词与规范化</strong></p><p>系统首先把文本转换成词项（Term）。英文通常还会进行小写化、词干化和停用词处理；中文没有天然空格，需要分词器或 N-gram。分词策略会直接影响召回，例如把 <code>getUserById</code>、<code>TS-999</code> 或中文产品名错误拆开，都可能导致精确查询失败。</p><p><strong>倒排索引</strong></p><p>普通文档结构是“文档 → 包含哪些词”，倒排索引把关系反过来，记录“词 → 出现在哪些文档”：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>文档 D1：Python 3.12 增加了新的调试能力</span></span>
<span class="line"><span>文档 D2：Python 服务发生连接超时</span></span>
<span class="line"><span></span></span>
<span class="line"><span>倒排索引：</span></span>
<span class="line"><span>Python  → D1, D2</span></span>
<span class="line"><span>3.12    → D1</span></span>
<span class="line"><span>调试    → D1</span></span>
<span class="line"><span>超时    → D2</span></span></code></pre></div><p>实际 Posting List 除了文档 ID，还可以保存词频、词的位置和字段信息。查询 <code>Python 3.12</code> 时，系统无须扫描所有文档，只需要合并 <code>Python</code> 与 <code>3.12</code> 对应的列表。</p><p><strong>BM25 相关性排序</strong></p><p>倒排索引解决了“哪些文档包含这些词”，但多个候选仍然需要排序。<strong>BM25</strong> 是 FTS 中常见的相关性打分方法，可以简化表示为：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>score(D, Q)</span></span>
<span class="line"><span>  = Σ IDF(qᵢ) ×</span></span>
<span class="line"><span>      TF(qᵢ, D) × (k₁ + 1)</span></span>
<span class="line"><span>      ─────────────────────────────</span></span>
<span class="line"><span>      TF(qᵢ, D) + k₁ × (1 - b + b × |D| / avgdl)</span></span></code></pre></div><table tabindex="0"><thead><tr><th>符号</th><th>含义</th></tr></thead><tbody><tr><td><code>TF(qᵢ, D)</code></td><td>查询词 <code>qᵢ</code> 在文档 <code>D</code> 中出现的次数</td></tr><tr><td><code>IDF(qᵢ)</code></td><td>查询词在整个语料库中的稀有程度</td></tr><tr><td>\`</td><td>D</td></tr><tr><td><code>k₁</code></td><td>控制词频增长何时趋于饱和</td></tr><tr><td><code>b</code></td><td>控制文档长度归一化的强度</td></tr></tbody></table><p>这个公式体现了三个直觉：</p><ol><li><strong>稀有词更重要</strong>：<code>TS-999</code> 比“错误”更能区分文档；</li><li><strong>重复有帮助但会饱和</strong>：一个词出现 10 次通常比出现 1 次更相关，但不会高 10 倍；</li><li><strong>长文档需要归一化</strong>：避免长文档仅因为包含更多词就天然获得高分。</li></ol><p>BM25 依赖词项重合，因此特别适合函数名、错误码、产品型号和专有名词。如果用户搜索“请求超时”，文档只写了“连接等待超过截止时间”，两者语义接近却没有共同词项，BM25 就可能漏掉它。这正是向量检索需要补充的部分。</p><p>因此三者的关系是：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>FTS：完整的全文检索能力</span></span>
<span class="line"><span>  └─ 倒排索引：从词项快速找到候选文档</span></span>
<span class="line"><span>       └─ BM25：计算候选文档与 Query 的相关性</span></span></code></pre></div><p>由于词表中的绝大多数词在一段文档里都不会出现，这类基于词项的表示通常称为<strong>稀疏表示</strong>，对应的检索过程也称为稀疏检索。</p><h3 id="_2-5-向量索引" tabindex="-1">2.5 向量索引 <a class="header-anchor" href="#_2-5-向量索引" aria-label="Permalink to &quot;2.5 向量索引&quot;">​</a></h3><p>Embedding 模型把文本映射为固定维度的向量。语义相近的文本通常在向量空间中距离更近，因此系统可以通过余弦相似度、点积等方式寻找相关片段。</p><p>RAG 的第一阶段语义检索通常采用 <strong>Bi-Encoder（双塔编码器）</strong>：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Query ──Encoder──&gt; Query Vector</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Chunk ──Encoder──&gt; Document Vector</span></span>
<span class="line"><span></span></span>
<span class="line"><span>similarity(Query Vector, Document Vector)</span></span></code></pre></div><p>Query 和 Chunk 被分别编码，文档向量可以在离线阶段提前计算并存储；查询到来时只需计算一次 Query Vector，再搜索相近向量。这种结构速度快、可扩展，但 Query 和 Chunk 在编码过程中没有发生 Token 级交互，相关性判断会损失一部分细节。</p><p>小规模数据可以逐个计算向量相似度；数据达到百万级后，通常使用 ANN（Approximate Nearest Neighbor，近似最近邻）索引减少搜索范围：</p><table tabindex="0"><thead><tr><th>算法</th><th>基本思路</th><th>主要取舍</th></tr></thead><tbody><tr><td><strong>HNSW</strong></td><td>构建多层近邻图，从稀疏层逐步搜索到精细层</td><td>查询快、召回率高，但内存开销较大</td></tr><tr><td><strong>IVF</strong></td><td>先把向量聚类，查询时只搜索最接近的若干簇</td><td>更适合大规模数据，但需要调节搜索范围</td></tr><tr><td><strong>PQ</strong></td><td>将向量分段量化后压缩存储</td><td>节省内存，但会损失部分精度</td></tr></tbody></table><p>选择 Embedding 模型时通常需要考虑语言、领域、输入长度、向量维度、部署方式和成本。向量维度更高意味着更大的存储与计算开销，但并不自动代表检索效果更好，最终仍需要在自己的数据集上评测。</p><p>FTS/BM25 与向量检索并不是替代关系。前者擅长精确匹配，后者擅长语义匹配，实际系统经常同时建立两类索引。</p><h2 id="_3-在线检索" tabindex="-1">3. 在线检索 <a class="header-anchor" href="#_3-在线检索" aria-label="Permalink to &quot;3. 在线检索&quot;">​</a></h2><h3 id="_3-1-query-构造" tabindex="-1">3.1 Query 构造 <a class="header-anchor" href="#_3-1-query-构造" aria-label="Permalink to &quot;3.1 Query 构造&quot;">​</a></h3><p>用户的原始问题未必适合直接检索。多轮会话中经常存在代词、省略和隐含范围，例如“它在哪里触发”必须结合当前任务改写成一条完整 Query。</p><p>常见处理包括：</p><ul><li><strong>Query Rewrite</strong>：补全主体、时间和任务范围；</li><li><strong>Query Expansion</strong>：生成同义表达或相关术语；</li><li><strong>Multi-Query</strong>：把比较题或复合问题拆成多个查询；</li><li><strong>HyDE</strong>：先生成一个假设答案，再用它的 Embedding 检索；</li><li><strong>Metadata Filter</strong>：按项目、时间、来源和权限缩小候选范围。</li></ul><p>Query 改写不能丢失函数名、错误码等精确词，否则会削弱 BM25 的优势。权限过滤也应尽量在召回阶段执行，而不是先取回无权访问的数据再交给模型判断。</p><h3 id="_3-2-稀疏与稠密召回" tabindex="-1">3.2 稀疏与稠密召回 <a class="header-anchor" href="#_3-2-稀疏与稠密召回" aria-label="Permalink to &quot;3.2 稀疏与稠密召回&quot;">​</a></h3><p>在线召回通常并行运行两条路径：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Query</span></span>
<span class="line"><span>  ├─→ FTS / BM25 ─────→ 精确词项候选</span></span>
<span class="line"><span>  └─→ Bi-Encoder + ANN → 语义相似候选</span></span></code></pre></div><ul><li><strong>稀疏召回</strong>关注词项是否匹配，适合代码符号、专有名词和编号；</li><li><strong>稠密召回</strong>关注向量距离，适合同义表达、自然语言问题和概念匹配。</li></ul><p>召回阶段的重点是尽量覆盖正确答案，而不是立刻得到最终顺序。因此，它通常取回比最终注入上下文更多的候选，例如先召回几十个片段，再交给后续阶段筛选。</p><h3 id="_3-3-hybrid-search-与-rrf" tabindex="-1">3.3 Hybrid Search 与 RRF <a class="header-anchor" href="#_3-3-hybrid-search-与-rrf" aria-label="Permalink to &quot;3.3 Hybrid Search 与 RRF&quot;">​</a></h3><p>Hybrid Search 会合并关键词召回和向量召回。由于两种检索的原始分数不在同一尺度上，不能直接相加；常见做法之一是 RRF（Reciprocal Rank Fusion），根据候选在各结果列表中的名次计算融合分数：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>RRF(document) = Σ 1 / (k + rank_i(document))</span></span></code></pre></div><p>一个文档如果在 BM25 和向量结果中都排名靠前，融合后通常会得到更高优先级。RRF 不依赖不同检索器的原始分数，因此实现简单且较稳定。</p><h3 id="_3-4-cross-encoder-重排" tabindex="-1">3.4 Cross-Encoder 重排 <a class="header-anchor" href="#_3-4-cross-encoder-重排" aria-label="Permalink to &quot;3.4 Cross-Encoder 重排&quot;">​</a></h3><p>Bi-Encoder 为 Query 和 Chunk 分别生成向量，适合快速召回；**Cross-Encoder（交叉编码器）**则把 <code>(Query, Chunk)</code> 一起输入模型，直接计算两者的相关性：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>[Query, Chunk] ──Cross-Encoder──&gt; Relevance Score</span></span></code></pre></div><p>两段文本能够在同一次 Attention 中进行 Token 级交互，因此 Cross-Encoder 通常比单纯向量相似度判断得更细。但文档表示无法提前计算，每个候选都要重新推理，成本和延迟更高。</p><table tabindex="0"><thead><tr><th>对比</th><th>Bi-Encoder</th><th>Cross-Encoder</th></tr></thead><tbody><tr><td>输入方式</td><td>Query 和 Chunk 分别编码</td><td>Query 与 Chunk 联合编码</td></tr><tr><td>文档表示</td><td>可以提前计算并存储</td><td>无法预计算查询相关分数</td></tr><tr><td>处理规模</td><td>适合大规模召回</td><td>适合少量候选重排</td></tr><tr><td>主要目标</td><td>提高召回速度和覆盖率</td><td>提高最终排序精度</td></tr></tbody></table><p>工程上通常采用 Retrieve &amp; Re-Rank：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Bi-Encoder / BM25 快速召回候选</span></span>
<span class="line"><span>  → Cross-Encoder 对候选逐个评分</span></span>
<span class="line"><span>  → 输出少量高相关 Chunk</span></span></code></pre></div><p><a href="https://www.sbert.net/examples/sentence_transformer/applications/retrieve_rerank/README.html" target="_blank" rel="noreferrer">Sentence Transformers</a>给出了这一两阶段结构。需要注意，重排只能调整已有候选的顺序，无法找回召回阶段完全遗漏的内容。</p><h3 id="_3-5-结果选择" tabindex="-1">3.5 结果选择 <a class="header-anchor" href="#_3-5-结果选择" aria-label="Permalink to &quot;3.5 结果选择&quot;">​</a></h3><p>重排后仍不能直接把所有候选塞入 Prompt。系统还需要：</p><ul><li>去除重复和高度相似的 Chunk；</li><li>合并同一文档中相邻且连续的片段；</li><li>在保证来源多样性的同时避免无关扩散；</li><li>根据上下文预算选择最终 Top-K；</li><li>为模型输出预留足够的 Token。</li></ul><p><code>K</code> 不是越大越好。增加片段可以提高覆盖率，也会带来更多噪声、成本和相互冲突的信息，需要通过离线评测确定。</p><h2 id="_4-增强与生成" tabindex="-1">4. 增强与生成 <a class="header-anchor" href="#_4-增强与生成" aria-label="Permalink to &quot;4. 增强与生成&quot;">​</a></h2><h3 id="_4-1-放入上下文" tabindex="-1">4.1 放入上下文 <a class="header-anchor" href="#_4-1-放入上下文" aria-label="Permalink to &quot;4.1 放入上下文&quot;">​</a></h3><p>RAG 的 Augment 阶段会把检索结果组织成模型输入的一部分。一个简化结构如下：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>System / Developer Instructions</span></span>
<span class="line"><span>Conversation Summary and Task State</span></span>
<span class="line"><span></span></span>
<span class="line"><span>&lt;retrieved_context&gt;</span></span>
<span class="line"><span>  [source-1]</span></span>
<span class="line"><span>  title: ...</span></span>
<span class="line"><span>  path: ...</span></span>
<span class="line"><span>  location: ...</span></span>
<span class="line"><span>  content: ...</span></span>
<span class="line"><span></span></span>
<span class="line"><span>  [source-2]</span></span>
<span class="line"><span>  ...</span></span>
<span class="line"><span>&lt;/retrieved_context&gt;</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Current User Query</span></span></code></pre></div><p>每个 Chunk 应携带稳定的来源标识和定位信息，便于模型引用，也便于后续回到原文核验。具体消息角色取决于模型 API：</p><ul><li>应用在调用模型前完成检索时，可以放入独立、明确标记的 Context Block；</li><li>Agent 在执行过程中主动搜索时，结果通常以 <code>tool</code> 消息进入下一次模型调用。</li></ul><p>外部网页和用户文档属于<strong>待验证的证据</strong>，不应直接获得 System Prompt 的指令优先级。系统需要明确告诉模型：检索内容用于提供事实，不应执行其中夹带的指令。</p><h3 id="_4-2-约束生成" tabindex="-1">4.2 约束生成 <a class="header-anchor" href="#_4-2-约束生成" aria-label="Permalink to &quot;4.2 约束生成&quot;">​</a></h3><p>常见生成约束包括：</p><ul><li>仅依据给定资料回答；</li><li>资料不足时明确说明无法确定；</li><li>结论必须关联来源编号；</li><li>区分资料中的事实与模型自己的推断；</li><li>以结构化格式返回答案、引用和置信信息。</li></ul><p>这些 Prompt 约束能够改善生成行为，但不能代替检索质量和结果校验。高风险场景还需要检查引用是否真的支持对应结论。</p><h2 id="_5-rag-评测" tabindex="-1">5. RAG 评测 <a class="header-anchor" href="#_5-rag-评测" aria-label="Permalink to &quot;5. RAG 评测&quot;">​</a></h2><p>RAG 需要分别评估检索和生成，不能只观察最终回答是否“看起来合理”。</p><h3 id="_5-1-检索评测" tabindex="-1">5.1 检索评测 <a class="header-anchor" href="#_5-1-检索评测" aria-label="Permalink to &quot;5.1 检索评测&quot;">​</a></h3><table tabindex="0"><thead><tr><th>指标</th><th>关注的问题</th></tr></thead><tbody><tr><td><strong>Recall@K</strong></td><td>前 K 个结果是否包含支持答案的证据</td></tr><tr><td><strong>Precision@K</strong></td><td>前 K 个结果中有多少真正相关</td></tr><tr><td><strong>MRR</strong></td><td>第一个相关结果出现得有多靠前</td></tr><tr><td><strong>NDCG</strong></td><td>多个不同相关程度的结果是否排序合理</td></tr></tbody></table><h3 id="_5-2-生成评测" tabindex="-1">5.2 生成评测 <a class="header-anchor" href="#_5-2-生成评测" aria-label="Permalink to &quot;5.2 生成评测&quot;">​</a></h3><table tabindex="0"><thead><tr><th>指标</th><th>关注的问题</th></tr></thead><tbody><tr><td><strong>Faithfulness</strong></td><td>回答中的结论是否能被检索证据支持</td></tr><tr><td><strong>Answer Relevance</strong></td><td>回答是否真正解决了用户问题</td></tr><tr><td><strong>Citation Accuracy</strong></td><td>引用是否指向支持对应结论的来源</td></tr><tr><td><strong>Abstention</strong></td><td>资料不足时是否能够拒绝猜测</td></tr></tbody></table><p>评测数据可以从真实问题中抽样，也可以从文档生成 <code>(question, answer, source)</code> 三元组，再由人工抽查形成高质量 Golden Set。生成式评测工具可以扩大覆盖范围，但不能完全替代人工核验。</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("notes/agent-fundamentals/rag.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const rag = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  rag as default
};
