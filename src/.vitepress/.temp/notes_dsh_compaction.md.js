import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"上下文压缩","description":"理解 DSH 如何在 Token 压力下剪枝工具结果、生成摘要并恢复模型上下文。","frontmatter":{"title":"上下文压缩","description":"理解 DSH 如何在 Token 压力下剪枝工具结果、生成摘要并恢复模型上下文。","date":"2026-09-01T00:00:00.000Z","tags":["DSH","Compaction"],"outline":[2,4]},"headers":[],"relativePath":"notes/dsh/compaction.md","filePath":"notes/dsh/compaction.md","lastUpdated":1788281304000}');
const _sfc_main = { name: "notes/dsh/compaction.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="上下文压缩" tabindex="-1">上下文压缩 <a class="header-anchor" href="#上下文压缩" aria-label="Permalink to &quot;上下文压缩&quot;">​</a></h1><p>Agent 的 Session 会不断变长，但是模型的上下文窗口是有限的。所以运行一段时间之后，系统总会遇到一个问题：历史还在，但模型已经不能把全部历史都放进下一次请求了。</p><p>DSH 的压缩模块要解决的不是简单地“删掉旧消息”，而是：在不丢失原始事实的前提下，减少模型下一次需要读取的内容。</p><h2 id="_1-先给结论-动作是两段-路径不止两种" tabindex="-1">1. 先给结论：动作是两段，路径不止两种 <a class="header-anchor" href="#_1-先给结论-动作是两段-路径不止两种" aria-label="Permalink to &quot;1. 先给结论：动作是两段，路径不止两种&quot;">​</a></h2><p>如果按照“实际做了什么压缩动作”来分，DSH 是两段式的；但如果按照“什么时候、以什么策略进入压缩”来分，就不能简单说只有两级。</p><ol><li><strong>第一级：工具结果剪枝</strong>。优先处理体积很大的旧工具输出，尽量不改变对话结构；</li><li><strong>第二级：历史摘要替换</strong>。如果剪枝之后仍然不够，就选择一段安全的旧历史生成摘要，用摘要替换 Model Surface 中的那段内容。</li></ol><p>所以它不是一上来就总结整段对话，而是先做成本更低、影响更小的处理。只有第一级释放的空间不够，才会进入第二级。这里的“二级”描述的是压缩动作，不代表整个模块只有两条运行路径。</p><h3 id="_1-1-触发位置和相关事件" tabindex="-1">1.1 触发位置和相关事件 <a class="header-anchor" href="#_1-1-触发位置和相关事件" aria-label="Permalink to &quot;1.1 触发位置和相关事件&quot;">​</a></h3><p>压缩通常发生在下一次模型请求之前，也就是 <code>agent/pre-step</code> waterfall 中。可以先通过下面这张表理解每个位置要做什么：</p><table tabindex="0"><thead><tr><th>位置 / 事件</th><th>什么时候发生</th><th>主要检查</th><th>处理结果</th></tr></thead><tbody><tr><td><code>agent/pre-step</code></td><td>每个 Step 准备请求时</td><td>Token 是否接近上下文上限</td><td>触发自动压缩，或者直接继续请求</td></tr><tr><td><code>tokenMeter</code></td><td><code>agent/pre-step</code> 检查压力时</td><td>消息、工具 Schema 和运行上下文一共占多少 Token</td><td>为后续选择压缩级别提供依据</td></tr><tr><td><code>compaction/prune</code></td><td>第一级剪枝发生时</td><td>哪些旧工具结果超过保留预算，且仍在当前 Model Surface 中</td><td>追加经过剪短的工具结果，重新测量</td></tr><tr><td><code>compaction/start</code></td><td>真正开始摘要前</td><td>当前 Session 是否已有未结束的压缩操作</td><td>建立一次压缩的锁和开始记录</td></tr><tr><td><code>compaction/summary</code></td><td>摘要模型返回之后</td><td>摘要是否生成成功、覆盖范围是否安全</td><td>保存摘要、覆盖范围和摘要调用信息</td></tr><tr><td><code>user/message(surfaceOp: replace)</code></td><td>摘要准备写回表层时</td><td>Tool Call / Tool Result 是否完整配对，替换范围是否有效</td><td>让摘要替换 Model Surface 中的旧历史</td></tr><tr><td><code>compaction/end</code></td><td>压缩流程结束时</td><td>这次压缩是成功还是失败</td><td>释放压缩锁，记录最终状态</td></tr><tr><td><code>agent/request-error</code></td><td>模型已经返回上下文溢出时</td><td>这次错误是否属于可恢复的 Context Overflow</td><td>走压缩后重试，而不是直接结束 Turn</td></tr></tbody></table><p>这里有一个容易混淆的地方：<code>compaction/start</code>、<code>compaction/summary</code> 和 <code>compaction/end</code> 都是 Session Log 中的记录事件，它们本身不会直接进入模型上下文；真正改变 Model Surface 的，是带 <code>surfaceOp: replace</code> 的 <code>user/message</code>。</p><h3 id="_1-2-三种触发方式-不等于三种压缩级别" tabindex="-1">1.2 三种触发方式，不等于三种压缩级别 <a class="header-anchor" href="#_1-2-三种触发方式-不等于三种压缩级别" aria-label="Permalink to &quot;1.2 三种触发方式，不等于三种压缩级别&quot;">​</a></h3><p>从“什么时候触发”来看，压缩又有三种入口：</p><ul><li><strong>自动压力触发</strong>：在 <code>agent/pre-step</code> 发现 Token 压力过高；</li><li><strong>溢出恢复触发</strong>：模型请求已经因为上下文溢出失败，在 <code>agent/request-error</code> 中补救；</li><li><strong>手动触发</strong>：用户执行 <code>/compact</code>，对当前历史主动做一次压缩。</li></ul><p>这三种入口最后可以复用同一个压缩引擎。也就是说，触发方式有三种，但压缩动作本身仍然按照“先剪枝、再摘要”的两段策略执行。</p><h3 id="_1-3-压力路径还可能进行多轮摘要" tabindex="-1">1.3 压力路径还可能进行多轮摘要 <a class="header-anchor" href="#_1-3-压力路径还可能进行多轮摘要" aria-label="Permalink to &quot;1.3 压力路径还可能进行多轮摘要&quot;">​</a></h3><p>在正常的压力压缩中，摘要并不一定只执行一次。系统会在摘要替换之后重新测量 Token：</p><ul><li>如果已经低于阈值，就结束这次压缩；</li><li>如果仍然高于阈值，就按照 <code>compactionRetries</code> 再尝试下一段历史；</li><li>默认配置允许首次压缩之外再进行一次尝试。</li></ul><p>这属于同一级摘要动作的多轮重试，不是新增了“第三级压缩”。所以更准确的说法是：<strong>两段压缩动作 + 三种触发入口 + 压力场景下可配置的多轮摘要。</strong></p><p>另外，上下文溢出恢复和手动 <code>/compact</code> 有自己的边界：</p><ul><li><code>agent/request-error</code> 收到模型确认的上下文溢出后，会绕过正常压力阈值和近期保留策略，强制寻找一次可以配对、可以缩小的平衡范围；</li><li><code>/compact</code> 可以在没有达到压力阈值时主动执行，通常要求 Agent 处于空闲状态；</li><li>它们仍然复用“剪枝 → 摘要替换”的动作，但不能和正常 <code>agent/pre-step</code> 压力路径混为一谈。</li></ul><h2 id="_2-自动压缩的触发位置" tabindex="-1">2. 自动压缩的触发位置 <a class="header-anchor" href="#_2-自动压缩的触发位置" aria-label="Permalink to &quot;2. 自动压缩的触发位置&quot;">​</a></h2><p>压缩是一个可选的插件能力，不属于 Agent Loop 的主干。</p><p>自动压缩通常在 <code>agent/pre-step</code> 中检查 Token 压力。因为这个位置正好在下一个模型请求之前，所以压缩完成后可以直接影响当前 Step 要看到的 Model Surface。</p><p>如果模型已经明确返回“上下文溢出”，系统也可以在 <code>agent/request-error</code> 中触发恢复，再重新发起请求。</p><p>手动执行 <code>/compact</code> 和自动压缩使用的是同一套能力，避免手动和自动形成两种不一样的压缩逻辑。</p><h2 id="_3-自动压缩的整体流程" tabindex="-1">3. 自动压缩的整体流程 <a class="header-anchor" href="#_3-自动压缩的整体流程" aria-label="Permalink to &quot;3. 自动压缩的整体流程&quot;">​</a></h2><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>agent/pre-step 检查 Token 压力</span></span>
<span class="line"><span>├── 未超过阈值 → 不做压缩，继续请求模型</span></span>
<span class="line"><span>└── 超过阈值</span></span>
<span class="line"><span>    → 先尝试剪枝工具结果</span></span>
<span class="line"><span>    → 重新测量 Token</span></span>
<span class="line"><span>    ├── 已经低于阈值 → 结束压缩，继续请求模型</span></span>
<span class="line"><span>    └── 仍然过高 → 选择安全历史范围</span></span>
<span class="line"><span>        → 调用模型生成摘要</span></span>
<span class="line"><span>        → 写入 compaction/summary</span></span>
<span class="line"><span>        → 用摘要替换 Model Surface</span></span>
<span class="line"><span>        → 重新测量，必要时再进行下一轮摘要</span></span>
<span class="line"><span></span></span>
<span class="line"><span>agent/request-error 收到上下文溢出</span></span>
<span class="line"><span>→ 绕过正常阈值和保留尾部策略</span></span>
<span class="line"><span>→ 剪枝并寻找一次平衡的可压缩范围</span></span>
<span class="line"><span>→ 摘要替换</span></span>
<span class="line"><span>→ 重新请求原模型调用</span></span></code></pre></div><h2 id="_4-第一级-先剪枝工具结果" tabindex="-1">4. 第一级：先剪枝工具结果 <a class="header-anchor" href="#_4-第一级-先剪枝工具结果" aria-label="Permalink to &quot;4. 第一级：先剪枝工具结果&quot;">​</a></h2><p>工具结果经常比普通对话更占空间，特别是文件内容、命令输出和长列表。</p><p>这些内容有时已经被后续的模型回答吸收，继续完整放在每一次请求里，成本比较高。因此压缩会先尝试剪枝旧的工具结果，再重新测量 Token。</p><p>如果剪枝已经释放了足够空间，就不需要生成一段完整的对话摘要。这是一种影响比较小的处理方式：保留对话结构，只缩短已经不太需要原样展示的工具内容。</p><h2 id="_5-第二级-选择可以压缩的历史范围" tabindex="-1">5. 第二级：选择可以压缩的历史范围 <a class="header-anchor" href="#_5-第二级-选择可以压缩的历史范围" aria-label="Permalink to &quot;5. 第二级：选择可以压缩的历史范围&quot;">​</a></h2><p>如果剪枝之后空间仍然不够，系统才会进一步选择历史范围生成摘要。</p><p>这里不能机械地截取“最早的前 N 条消息”，因为工具调用有自己的结构。一个 Tool Call 和它对应的 Tool Result 需要完整配对，不能从中间切开。</p><p>选择范围时还需要避开当前正在执行的部分，只处理已经完成、可以稳定归纳的历史。否则摘要可能把一个还没有结果的工具调用描述成已经完成。</p><p>可以把这一步理解为：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>找到比较旧的历史</span></span>
<span class="line"><span>→ 检查消息是否完整</span></span>
<span class="line"><span>→ 检查 Tool Call / Tool Result 是否配对</span></span>
<span class="line"><span>→ 避开当前正在运行的 Step</span></span>
<span class="line"><span>→ 确定可以交给摘要模型的范围</span></span></code></pre></div><h2 id="_6-生成摘要并替换表层" tabindex="-1">6. 生成摘要并替换表层 <a class="header-anchor" href="#_6-生成摘要并替换表层" aria-label="Permalink to &quot;6. 生成摘要并替换表层&quot;">​</a></h2><p>确定范围之后，系统会调用模型生成摘要，并记录压缩过程：</p><ul><li><code>compaction/start</code>：开始一次压缩；</li><li><code>compaction/summary</code>：保存生成的摘要；</li><li><code>compaction/end</code>：压缩完成或结束。</li></ul><p>然后系统会追加一条带有 <code>surfaceOp: replace</code> 的 <code>user/message</code>，告诉消息派生逻辑：摘要可以在模型表层替换之前那段历史。</p><p>下一次生成模型请求时，模型看到的就会变成：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>压缩摘要</span></span>
<span class="line"><span>+ 最近仍然需要的完整消息</span></span>
<span class="line"><span>+ 新到达的用户输入和工具结果</span></span></code></pre></div><h2 id="_7-替换的是-model-surface-不是-session-log" tabindex="-1">7. 替换的是 Model Surface，不是 Session Log <a class="header-anchor" href="#_7-替换的是-model-surface-不是-session-log" aria-label="Permalink to &quot;7. 替换的是 Model Surface，不是 Session Log&quot;">​</a></h2><p>这里最容易混淆的一点是：压缩不会删除原始事件。</p><p>DSH 实际上有两层历史：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Session Event Log</span></span>
<span class="line"><span>└── 完整、只追加的权威事实</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Model Surface</span></span>
<span class="line"><span>└── 下一次请求实际展示给模型的历史投影</span></span></code></pre></div><p>带 <code>surfaceOp: replace</code> 的消息，只改变 Model Surface。被摘要覆盖的旧事件仍然留在 Session Log 中，以后仍然可以用于：</p><ul><li>回放完整过程；</li><li>审计某次模型决策；</li><li>Fork 一个新的 Agent；</li><li>排查摘要是否遗漏了信息；</li><li>重新派生其他查询视图。</li></ul><div class="tip custom-block"><p class="custom-block-title">这不是删除聊天记录</p><p>压缩减少的是模型的认知负担，不是系统的事实留存。即使摘要后来被发现不够准确，原始事件仍然在权威日志中，没有被永久删除。</p></div><h2 id="_8-压缩过程也需要可恢复" tabindex="-1">8. 压缩过程也需要可恢复 <a class="header-anchor" href="#_8-压缩过程也需要可恢复" aria-label="Permalink to &quot;8. 压缩过程也需要可恢复&quot;">​</a></h2><p>压缩本身也可能被中断，比如摘要请求还没有返回时进程退出。因为压缩的开始、摘要和结束都会写入 Session Log，所以恢复时可以检查是否存在只有 <code>compaction/start</code>、却没有完成事件的记录。</p><p>这时系统可以识别出一次未完成的压缩，而不是把它当作普通历史继续使用。</p><p>同样地，当模型请求确认上下文溢出时，也可以走“压缩后重试”的路径，而不是让整次 Turn 直接失败。</p><h2 id="_9-token-meter-的作用" tabindex="-1">9. Token Meter 的作用 <a class="header-anchor" href="#_9-token-meter-的作用" aria-label="Permalink to &quot;9. Token Meter 的作用&quot;">​</a></h2><p>压缩是否发生，需要先知道当前请求到底有多大。Token Meter 负责估计：</p><ul><li>当前 Model Surface 中的消息；</li><li>当前可见的工具 Schema；</li><li>其他会进入模型请求的上下文。</li></ul><p>Compaction 根据这个测量结果选择策略。两者分开以后，更换模型或 Tokenizer 时，只需要调整计量方式，不需要重写压缩的业务流程。</p><div class="tip custom-block"><p class="custom-block-title">章节小结</p><p>DSH 的上下文压缩不是“删掉旧消息”，而是先尝试剪枝工具结果，再在安全边界内生成摘要，最后用替换语义改变模型看到的表层。原始 Session Log 一直保留，所以 Agent 可以继续工作，系统也仍然记得发生过什么。</p></div></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("notes/dsh/compaction.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const compaction = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  compaction as default
};
