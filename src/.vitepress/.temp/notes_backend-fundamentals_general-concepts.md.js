import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"通用基础","description":"整理无法单独归入网络、操作系统、数据库等方向的后端通用知识。","frontmatter":{"title":"通用基础","description":"整理无法单独归入网络、操作系统、数据库等方向的后端通用知识。","outline":[2,4]},"headers":[],"relativePath":"notes/backend-fundamentals/general-concepts.md","filePath":"notes/backend-fundamentals/general-concepts.md","lastUpdated":1788345156000}');
const _sfc_main = { name: "notes/backend-fundamentals/general-concepts.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="通用基础" tabindex="-1">通用基础 <a class="header-anchor" href="#通用基础" aria-label="Permalink to &quot;通用基础&quot;">​</a></h1><h2 id="_1-设计原则-solid" tabindex="-1">1. 设计原则：SOLID <a class="header-anchor" href="#_1-设计原则-solid" aria-label="Permalink to &quot;1. 设计原则：SOLID&quot;">​</a></h2><p>SOLID 是面向对象设计中五条常见原则的缩写。它们关注的不是把代码拆得越细越好，而是控制职责、依赖和变化的影响范围，使系统更容易理解、扩展和测试。</p><h3 id="_1-1-单一职责原则-srp" tabindex="-1">1.1 单一职责原则（SRP） <a class="header-anchor" href="#_1-1-单一职责原则-srp" aria-label="Permalink to &quot;1.1 单一职责原则（SRP）&quot;">​</a></h3><p>单一职责原则指一个类或模块应该只负责一类职责，最好只有一个引起它变化的原因。它的目的不是机械地把类拆得越小越好，而是降低修改的影响范围，提高代码的可维护性、可读性和可测试性。</p><p>例如在订单系统中，如果 <code>OrderService</code> 同时负责价格计算、库存扣减、订单创建、消息通知和报表统计，它就承担了过多职责。更合适的做法是把价格、库存和通知等逻辑交给独立组件，<code>OrderService</code> 只负责下单流程的编排。</p><h3 id="_1-2-开闭原则-ocp" tabindex="-1">1.2 开闭原则（OCP） <a class="header-anchor" href="#_1-2-开闭原则-ocp" aria-label="Permalink to &quot;1.2 开闭原则（OCP）&quot;">​</a></h3><p>开闭原则指软件实体应该<strong>对扩展开放，对修改关闭</strong>。当系统增加一种新能力时，应该尽量通过新增实现来完成，而不是反复修改已经稳定的核心流程，从而减少回归风险。</p><p>例如支付系统支持支付宝和微信支付时，如果 <code>PaymentService</code> 使用一个不断增长的 <code>if-else</code> 判断支付类型，那么每增加一种支付方式都要修改它。更合适的做法是定义统一的 <code>PaymentMethod</code> 接口，每种支付方式提供自己的实现，核心流程只负责选择并调用对应实现。</p><h3 id="_1-3-里氏替换原则-lsp" tabindex="-1">1.3 里氏替换原则（LSP） <a class="header-anchor" href="#_1-3-里氏替换原则-lsp" aria-label="Permalink to &quot;1.3 里氏替换原则（LSP）&quot;">​</a></h3><p>里氏替换原则指子类型必须能够替换父类型，并且不破坏调用方原有的正确性。子类型不仅要满足相同的方法签名，还要遵守父类型承诺的行为，例如输入约束、返回结果和异常语义。</p><p>例如系统定义了支持 <code>save</code> 和 <code>get</code> 的 <code>Storage</code>，本地存储和对象存储都应该能够正常实现这两个操作。如果某个只读存储继承 <code>Storage</code> 后，在调用 <code>save</code> 时直接抛出“不支持”异常，它就不能真正替换父类型。此时应拆分读写接口，而不是建立不恰当的继承关系。</p><h3 id="_1-4-接口隔离原则-isp" tabindex="-1">1.4 接口隔离原则（ISP） <a class="header-anchor" href="#_1-4-接口隔离原则-isp" aria-label="Permalink to &quot;1.4 接口隔离原则（ISP）&quot;">​</a></h3><p>接口隔离原则指调用方不应该依赖自己不需要的方法。与其设计一个包含大量能力的通用接口，不如按照使用场景拆成多个小而明确的接口，避免一个无关方法的变化影响所有实现者。</p><p>例如一个 <code>UserService</code> 接口同时包含用户查询、修改、数据导出和邮件发送，仅读取用户信息的调用方也会被迫依赖后面三类能力。可以将它拆成 <code>UserReader</code>、<code>UserWriter</code>、<code>UserExporter</code> 等接口，让调用方只依赖实际使用的部分。</p><h3 id="_1-5-依赖倒置原则-dip" tabindex="-1">1.5 依赖倒置原则（DIP） <a class="header-anchor" href="#_1-5-依赖倒置原则-dip" aria-label="Permalink to &quot;1.5 依赖倒置原则（DIP）&quot;">​</a></h3><p>依赖倒置原则指高层业务逻辑不应该直接依赖具体的底层实现，高层和底层都应该依赖稳定的抽象。这样底层技术发生变化时，核心业务流程不需要随之修改，也更容易在测试中替换外部依赖。</p><p>例如 <code>OrderService</code> 如果直接创建某个支付 SDK 的客户端，业务逻辑就会与该供应商绑定。更合适的做法是让它依赖 <code>PaymentGateway</code> 接口，再由外部注入具体实现。切换支付供应商或编写单元测试时，只需替换实现，不必修改订单流程。</p><h2 id="_2-常用设计模式" tabindex="-1">2. 常用设计模式 <a class="header-anchor" href="#_2-常用设计模式" aria-label="Permalink to &quot;2. 常用设计模式&quot;">​</a></h2><p>设计模式是对一类重复设计问题的通用解决方案。它描述对象应该如何分工与协作，而不是一段可以直接复制的固定代码。只有当相应的变化和复杂度真实存在时，使用设计模式才有价值。</p><h3 id="_2-1-工厂模式" tabindex="-1">2.1 工厂模式 <a class="header-anchor" href="#_2-1-工厂模式" aria-label="Permalink to &quot;2.1 工厂模式&quot;">​</a></h3><p>工厂模式把<strong>使用对象</strong>与<strong>创建对象</strong>分开：调用方只表达自己需要什么，不直接决定具体类型如何构造。</p><p>例如支付系统同时支持支付宝和微信支付，可以让两种支付实现遵守同一个 <code>PaymentMethod</code> 接口，再由工厂根据支付类型返回对应实现：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>OrderService</span></span>
<span class="line"><span>    ↓ 请求 paymentType</span></span>
<span class="line"><span>PaymentFactory</span></span>
<span class="line"><span>    ├─ alipay → AlipayPayment</span></span>
<span class="line"><span>    └─ wechat → WechatPayment</span></span></code></pre></div><p>这样 <code>OrderService</code> 不需要知道 SDK 客户端如何初始化，也不负责管理不同支付实现的构造参数。对象创建逻辑发生变化时，业务流程通常不需要修改。</p><p>工厂模式有几个常见层次：</p><ul><li><strong>简单工厂</strong>：由一个工厂方法通过条件判断创建不同对象。实现简单，但新增类型仍要修改工厂；它不属于 GoF 23 种模式。</li><li><strong>工厂方法</strong>：为每类产品提供相应工厂，通过新增工厂和产品实现扩展。</li><li><strong>抽象工厂</strong>：负责创建一组相互关联的对象，例如同时创建某个云厂商的存储、消息和监控客户端。</li></ul><p>如果对象构造非常简单、实现类型也不会变化，直接创建通常更清楚，不必为了模式额外增加工厂层。</p><h3 id="_2-2-策略模式" tabindex="-1">2.2 策略模式 <a class="header-anchor" href="#_2-2-策略模式" aria-label="Permalink to &quot;2.2 策略模式&quot;">​</a></h3><p>策略模式把一组可以互相替换的算法封装为独立实现，调用方通过统一接口使用它们。它解决的是<strong>同一个业务目标存在多种实现方式</strong>的问题。</p><p>例如电商系统可能同时存在普通会员、VIP 和促销活动三种计价规则：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>PriceService</span></span>
<span class="line"><span>    ↓ 使用 PricingStrategy</span></span>
<span class="line"><span>    ├─ NormalPricing</span></span>
<span class="line"><span>    ├─ VipPricing</span></span>
<span class="line"><span>    └─ PromotionPricing</span></span></code></pre></div><p><code>PriceService</code> 只负责调用当前策略，不需要在核心流程中维护不断增长的 <code>if-else</code>。具体策略可以由配置、用户类型或工厂在运行时选择。</p><p>策略模式与工厂模式经常配合使用，但两者目的不同：<strong>工厂负责创建哪个对象，策略负责执行哪种行为</strong>。如果算法只有一种且没有变化预期，引入策略接口只会增加不必要的间接层。</p><h3 id="_2-3-单例模式" tabindex="-1">2.3 单例模式 <a class="header-anchor" href="#_2-3-单例模式" aria-label="Permalink to &quot;2.3 单例模式&quot;">​</a></h3><p>单例模式保证一个类在特定作用域内只有一个实例，并提供统一的获取方式。它适合管理进程内需要共享且创建成本较高的对象，例如配置中心客户端、指标注册器或连接池管理器。</p><p>实现单例通常需要处理两个问题：</p><ol><li><strong>唯一创建</strong>：构造过程不能被调用方任意执行；</li><li><strong>并发安全</strong>：多个线程同时首次访问时，不能创建多个实例。</li></ol><p>常见实现包括启动时直接创建的饿汉式、首次使用时创建的懒汉式，以及双重检查锁。双重检查锁还必须依赖语言提供的内存可见性保证；例如 Java 通常需要配合 <code>volatile</code>，Go 通常使用 <code>sync.Once</code>。</p><p>单例也有明显边界：</p><ul><li>单例对象的创建是线程安全的，不代表它内部的可变状态也是线程安全的；</li><li>进程内只有一个实例，不代表分布式系统中全局只有一个实例；</li><li>隐式全局状态会增加测试和并发推理难度，依赖注入容器管理的单例作用域通常更容易控制。</li></ul><h3 id="_2-4-观察者模式" tabindex="-1">2.4 观察者模式 <a class="header-anchor" href="#_2-4-观察者模式" aria-label="Permalink to &quot;2.4 观察者模式&quot;">​</a></h3><p>观察者模式在一个对象状态发生变化时，通知所有订阅者执行相应操作。发布者只维护统一的观察者接口，不需要了解每个订阅者的具体逻辑。</p><p>例如订单创建后，需要分别执行积分发放、消息通知和数据统计：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>OrderCreated</span></span>
<span class="line"><span>    ├─ PointsObserver</span></span>
<span class="line"><span>    ├─ NotificationObserver</span></span>
<span class="line"><span>    └─ AnalyticsObserver</span></span></code></pre></div><p>增加一种后续处理时，只需注册新的观察者，不必修改订单创建的核心流程。这可以降低发布者与多个后续动作之间的直接耦合。</p><p>经典观察者模式通常发生在同一进程内，发布者直接持有并调用观察者；发布订阅模式通常引入事件总线或消息中间件，使发布者和订阅者不必直接认识。因此，消息队列可以实现类似的事件协作，但不等同于经典观察者模式。</p><p>使用观察者模式时还要考虑通知顺序、异常隔离、重复消费和订阅者注销。对于支付、扣款等关键流程，不能只“发出通知就不管”，还需要事务消息、重试或幂等机制保证结果可靠。</p><h3 id="_2-5-模式对比" tabindex="-1">2.5 模式对比 <a class="header-anchor" href="#_2-5-模式对比" aria-label="Permalink to &quot;2.5 模式对比&quot;">​</a></h3><table tabindex="0"><thead><tr><th>模式</th><th>核心问题</th><th>变化点</th></tr></thead><tbody><tr><td>工厂</td><td>应该创建哪个对象</td><td>对象类型和构造过程</td></tr><tr><td>策略</td><td>应该执行哪种算法</td><td>业务规则和算法</td></tr><tr><td>单例</td><td>如何管理唯一共享实例</td><td>对象生命周期</td></tr><tr><td>观察者</td><td>状态变化后通知谁</td><td>事件订阅关系</td></tr></tbody></table></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("notes/backend-fundamentals/general-concepts.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const generalConcepts = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  generalConcepts as default
};
